<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%=title%></title>
</head>
<body>
    <%- include('partials/nav') %>
    <h1><%= title%></h1>
    <% if(user) { %>
        <div class="profileInfo">
        <img src="<%= user.picture %>">
        <p class="name">
            <%= "Hi"+" "+user.nickname.charAt(0).toUpperCase()+user.nickname.slice(1) %>
        </p>
        </div>
    <% } %>
</body>
</html>